---
phase: 01-stability-and-cowork-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pytest.ini
  - tests/__init__.py
  - tests/test_stab.py
  - scripts/macro_analysis.py
  - scripts/market_data.py
  - scripts/parse_portfolio.py
autonomous: true
requirements:
  - STAB-01
  - STAB-02
  - STAB-03

must_haves:
  truths:
    - "Pipeline produces no pandas deprecation warnings or reindex-related errors"
    - "Cache write/load failures produce visible [WARN] messages instead of silent pass"
    - "matplotlib backend is Agg after importing visualize module"
  artifacts:
    - path: "scripts/macro_analysis.py"
      provides: "Fixed reindex call and 4 silent exception sites"
      contains: ".reindex(treasury_aligned.index).ffill()"
    - path: "scripts/market_data.py"
      provides: "Fixed 1 silent exception site in _load_cache"
      contains: "[WARN] Cache"
    - path: "scripts/parse_portfolio.py"
      provides: "Fixed 1 silent exception site in _detect_format"
      contains: "[WARN]"
    - path: "tests/test_stab.py"
      provides: "Unit tests for STAB-01, STAB-02, STAB-03"
    - path: "pytest.ini"
      provides: "Test configuration"
  key_links:
    - from: "tests/test_stab.py"
      to: "scripts/macro_analysis.py"
      via: "import and test reindex fix + exception handling"
      pattern: "from scripts"
---

<objective>
Create test infrastructure and fix all stability bugs: deprecated pandas reindex API, silent exception swallowing across 6 sites, and verify matplotlib Agg backend placement.

Purpose: Establish a reliable, warning-free baseline so Phase 2+ code builds on stable ground.
Output: pytest scaffold, passing stability tests, clean script execution.
</objective>

<execution_context>
@C:/Users/skimb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/skimb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-stability-and-cowork-hardening/01-CONTEXT.md
@.planning/phases/01-stability-and-cowork-hardening/01-RESEARCH.md
@.planning/phases/01-stability-and-cowork-hardening/01-VALIDATION.md

@scripts/macro_analysis.py
@scripts/market_data.py
@scripts/parse_portfolio.py
@scripts/visualize.py
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test scaffold and write stability tests</name>
  <files>pytest.ini, tests/__init__.py, tests/test_stab.py</files>
  <behavior>
    - test_reindex_ffill: Calling macro_analysis functions that use reindex produces correct results without using deprecated method= parameter
    - test_no_reindex_warning: No FutureWarning or DeprecationWarning emitted during reindex operations
    - test_cache_warn_on_failure: When cache write/load fails (e.g., permission error, corrupt pickle), a [WARN] message is printed to stdout -- not silently swallowed
    - test_pipeline_survives_cache_failure: Pipeline functions complete successfully even when cache operations raise exceptions
    - test_matplotlib_agg_backend: After importing scripts.visualize, matplotlib.get_backend() returns 'agg' (case-insensitive)
  </behavior>
  <action>
    1. Create `pytest.ini` with minimal config:
       ```ini
       [pytest]
       testpaths = tests
       python_files = test_*.py
       python_functions = test_*
       ```
    2. Create empty `tests/__init__.py`.
    3. Create `tests/test_stab.py` with tests for all 5 behaviors listed above.
       - For reindex tests: import `macro_analysis` functions that use reindex and verify no warnings with `warnings.catch_warnings(record=True)`.
       - For cache warn tests: monkeypatch the pickle/file operations to raise exceptions, capture stdout with `capsys`, assert "[WARN]" appears in output.
       - For cache survival test: monkeypatch cache to fail, call the function, assert it returns a valid result (not raises).
       - For matplotlib test: import `scripts.visualize`, then check `matplotlib.get_backend().lower() == 'agg'`.
       - These tests should FAIL initially (RED phase) because the fixes are not yet applied.
    4. Run `pip install pytest` if not already installed.
    5. Run `python -m pytest tests/test_stab.py -x -q` -- expect failures (RED).
  </action>
  <verify>
    <automated>python -m pytest tests/test_stab.py --co -q</automated>
  </verify>
  <done>pytest.ini exists, tests/test_stab.py contains 5 test functions, all tests are collected by pytest (may fail -- RED phase)</done>
</task>

<task type="auto">
  <name>Task 2: Apply stability fixes across all modules</name>
  <files>scripts/macro_analysis.py, scripts/market_data.py, scripts/parse_portfolio.py</files>
  <action>
    **STAB-01 -- Fix pandas reindex deprecation (1 site):**
    - `scripts/macro_analysis.py` line ~242: Change `pe_df['pe_ratio'].reindex(treasury_aligned.index, method='ffill')` to `pe_df['pe_ratio'].reindex(treasury_aligned.index).ffill()`

    **STAB-02 -- Replace ALL silent exceptions (6 sites):**
    All sites use `print()` with `[WARN]` prefix (matches existing codebase pattern -- no logging module).

    Cache-related (warn and continue):
    1. `scripts/macro_analysis.py` lines ~312-313 (`_add_to_pe_history`): Replace `except Exception: pass` with `except Exception as e: print(f"  [WARN] PE history save failed: {e}")`
    2. `scripts/macro_analysis.py` lines ~349-350 (`_save_cache`): Replace `except Exception: pass` with `except Exception as e: print(f"  [WARN] Cache save failed: {e}")`
    3. `scripts/market_data.py` lines ~164-165 (`_load_cache`): Replace silent except with `except Exception as e: print(f"  [WARN] Cache load failed: {e}")` before returning None

    Non-cache silent sites:
    4. `scripts/parse_portfolio.py` lines ~70-71 (`_detect_format`): Replace silent except with `except Exception as e: print(f"  [WARN] Format detection read failed: {e}")` and fall through to generic
    5. `scripts/macro_analysis.py` lines ~323-324 (`_load_pe_history`): Replace silent except with `except Exception as e: print(f"  [WARN] PE history load failed: {e}")` and return `[]`
    6. `scripts/macro_analysis.py` lines ~340-341 (`_load_cache`): Replace silent except with `except Exception as e: print(f"  [WARN] Cache load failed: {e}")` and return None

    **STAB-03 -- Verify matplotlib Agg backend:**
    - Confirm `scripts/visualize.py` already has `matplotlib.use('Agg')` before pyplot import (lines 10-12).
    - Grep all scripts/ files for any rogue `import matplotlib.pyplot` outside visualize.py. If found, add `matplotlib.use('Agg')` before that import. Research indicates none exist.

    After all fixes, run tests:
    ```
    python -m pytest tests/test_stab.py -x -v
    ```
    All 5 tests must pass (GREEN).
  </action>
  <verify>
    <automated>python -m pytest tests/test_stab.py -x -v</automated>
  </verify>
  <done>All 5 stability tests pass. No silent except Exception: pass remains in scripts/. pandas reindex uses chained .ffill() syntax. matplotlib Agg backend confirmed.</done>
</task>

</tasks>

<verification>
- `python -m pytest tests/test_stab.py -v` -- all 5 tests green
- `grep -rn "except Exception:" scripts/ | grep -v "[WARN]\|as e"` -- returns nothing (no silent exceptions remain)
- `grep -n "method='ffill'" scripts/` -- returns nothing (deprecated API removed)
</verification>

<success_criteria>
1. All 5 tests in test_stab.py pass
2. Zero silent `except Exception: pass` patterns in scripts/
3. No pandas deprecation warnings when importing and running macro_analysis functions
4. matplotlib backend is 'agg' after importing visualize
</success_criteria>

<output>
After completion, create `.planning/phases/01-stability-and-cowork-hardening/01-01-SUMMARY.md`
</output>
