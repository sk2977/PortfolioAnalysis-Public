---
phase: 04-qualitative-narrative-layer
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - README.md
  - tests/test_qual.py
autonomous: true
requirements:
  - COWK-03
  - QUAL-01
  - QUAL-02
  - QUAL-03
  - QUAL-04

must_haves:
  truths:
    - "CLAUDE.md contains a narrative generation step between analysis and report phases"
    - "CLAUDE.md instructs Claude to cite actual ERP and P/E values in macro narrative"
    - "CLAUDE.md instructs Claude to generate holding commentary only for abs(diff) > 0.05"
    - "CLAUDE.md instructs Claude to compute method spread and disclose when material"
    - "CLAUDE.md shows generate_report() call with macro_narrative, holding_commentary, method_spread_note params"
    - "README contains Cowork-specific setup instructions with clone, link, and upload steps"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Updated workflow with Phase 5.5 qualitative narrative step"
      contains: "macro_narrative"
    - path: "README.md"
      provides: "Cowork setup section for new users"
      contains: "Cowork"
    - path: "tests/test_qual.py"
      provides: "README content assertion test for COWK-03"
      contains: "test_readme_cowork_section"
  key_links:
    - from: "CLAUDE.md"
      to: "scripts/report.py"
      via: "Phase 5.5 narrative step feeds generate_report() call in Phase 6"
      pattern: "generate_report.*macro_narrative"
    - from: "README.md"
      to: "CLAUDE.md"
      via: "Setup instructions reference the workflow"
      pattern: "Cowork"
---

<objective>
Update CLAUDE.md with qualitative narrative generation workflow and update README.md with Cowork setup instructions.

Purpose: CLAUDE.md is the prompt that tells Claude when and how to generate narrative text during a Cowork session. README gives new users step-by-step Cowork setup. Without these, the report.py extensions from Plan 01 have no caller.
Output: Updated CLAUDE.md with Phase 5.5 narrative step, updated README.md with Cowork section, test for README content.
</objective>

<execution_context>
@C:/Users/skimb/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/skimb/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@CLAUDE.md
@README.md
@.planning/phases/04-qualitative-narrative-layer/04-RESEARCH.md
</context>

<interfaces>
<!-- From 04-RESEARCH.md, the CLAUDE.md addition pattern: -->

<!-- New Phase 5.5 step goes between Phase 4.5 (Structured Validation) and Phase 5 (Visualization) -->
<!-- Must reference: macro_narrative, holding_commentary, method_spread_note -->
<!-- Must instruct Claude to cite actual ERP value and P/E in macro narrative -->
<!-- Must specify >5% threshold for holding commentary -->
<!-- Must show generate_report() call with all narrative params -->

<!-- Phase 6 report call must be updated from: -->
<!-- report_md = generate_report(results, macro, portfolio, charts) -->
<!-- to include narrative params -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add COWK-03 README test and update README with Cowork setup section</name>
  <files>tests/test_qual.py, README.md</files>
  <action>
    **Step 1:** Append test_readme_cowork_section to tests/test_qual.py (if the file already exists from Plan 01 running first, append; if not, create with just this test plus necessary imports).

    The test should:
    ```python
    def test_readme_cowork_section():
        """COWK-03: README contains Cowork setup instructions."""
        readme_path = Path(__file__).parent.parent / 'README.md'
        content = readme_path.read_text(encoding='utf-8')
        assert 'Cowork' in content, "README must mention Cowork"
        assert 'clone' in content.lower(), "README must include clone step"
        assert 'upload' in content.lower() or 'portfolio' in content.lower(), "README must mention uploading portfolio"
    ```

    **Step 2:** Update README.md. Add a "## Getting Started with Claude Cowork" section between "## Setup" and "## Usage". Content:

    ```markdown
    ## Getting Started with Claude Cowork

    Cowork is Claude's built-in code execution environment. It runs the Python scripts in this project directly -- no API key needed.

    1. **Clone and install** (see Setup above)

    2. **Open Claude Desktop** and start a new conversation

    3. **Link the project folder**
       - Click "Add to project" (or drag the folder into Claude Desktop)
       - Select the cloned `PortfolioAnalysis-Public` directory

    4. **Upload your portfolio**
       - Click the attachment button and upload your CSV or XLSX file
       - Or describe your holdings in plain text (e.g., "40% VTI, 30% BND, 30% QQQ")

    5. **Start the analysis**
       - Type: "Run portfolio analysis using the uploaded file"
       - Or use the prompts in [prompt.md](prompt.md)

    Claude will run each pipeline step, ask about your risk tolerance, and deliver a report with macro context, optimized allocations, and qualitative commentary.
    ```

    **Step 3:** Run test to confirm.

    IMPORTANT: Do NOT use Unicode emojis anywhere. Keep ASCII-only per project conventions.
  </action>
  <verify>
    <automated>python -m pytest tests/test_qual.py::test_readme_cowork_section -x -q</automated>
  </verify>
  <done>
    - README.md contains "Getting Started with Claude Cowork" section
    - Section includes clone, link project, upload portfolio, and start analysis steps
    - test_readme_cowork_section passes
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 5.5 qualitative narrative step to CLAUDE.md and update Phase 6 report call</name>
  <files>CLAUDE.md</files>
  <action>
    **Step 1:** Insert a new "### Phase 5.5: Qualitative Narrative" section in CLAUDE.md between "### Phase 5: Visualization" and "### Phase 6: Report". This section instructs Claude to generate three narrative strings before calling generate_report().

    Content for Phase 5.5:

    ```markdown
    ### Phase 5.5: Qualitative Narrative

    Before calling generate_report(), generate three narrative strings. You (Claude) produce these by reading the structured data -- no API call needed.

    **1. Macro narrative (macro_narrative):**
    Read macro['erp'], macro['pe_ratio'], macro['treasury_yield'], and macro['interpretation'].
    Write 2-3 sentences interpreting the current macro regime in plain language.
    You MUST cite the actual ERP value and P/E ratio in your text.

    Example style:
    > "At a P/E of 27.4, the S&P 500 earnings yield is 3.6%, below the 10Y Treasury at 4.3%, producing an ERP of -0.7%. This negative premium suggests equities offer no compensation over risk-free bonds at current valuations."

    **2. Holding commentary (holding_commentary):**
    From results['comparison'], identify tickers where abs(Difference) > 0.05 (5 percentage points).
    For each, write one sentence explaining the direction and magnitude of the recommended change.
    Return as a dict: {"TICKER": "commentary sentence"}.
    Only include material changes -- skip tickers with smaller shifts.

    Do NOT use investment advice language ("you should buy/sell"). Use observational language ("the optimizer suggests increasing", "the model recommends reducing").

    **3. Method spread note (method_spread_note):**
    From results['returns_dict'], compare CAPM, mean historical, and EMA expected returns per ticker.
    If any ticker's spread (max - min across methods) exceeds 5 percentage points (0.05):

    Set method_spread_note to a string like:
    > "[TICKER]'s expected return ranges from X.X% (CAPM) to Y.Y% (EMA) -- a spread of Z.Zpp. Treat this ticker's recommendation as lower confidence."

    If multiple tickers exceed the threshold, mention each.
    If no ticker exceeds the threshold, set method_spread_note = None.

    Store all three as Python variables for the next step.
    ```

    **Step 2:** Update "### Phase 6: Report" section. Replace the existing generate_report() call with:

    ```python
    from scripts.report import generate_report
    report_md = generate_report(results, macro, portfolio, charts,
                                 macro_narrative=macro_narrative,
                                 holding_commentary=holding_commentary,
                                 method_spread_note=method_spread_note)
    print(report_md)
    ```

    Keep the existing narrative presentation instructions ("Present the key findings...") but add a 5th bullet:
    - "5. Qualitative commentary (macro interpretation, holding notes, confidence indicators)"

    **Step 3:** Verify the CLAUDE.md is well-formed by reading it back and checking the section order:
    Phase 1 -> Phase 2 -> Phase 3 -> Phase 4 -> Phase 4.5 -> Phase 5 -> Phase 5.5 -> Phase 6 -> Phase 7

    IMPORTANT: Do NOT use Unicode emojis. Keep ASCII-only. Do NOT add any external API calls or library imports. Claude IS the AI layer.
  </action>
  <verify>
    <automated>python -c "
content = open('CLAUDE.md', encoding='utf-8').read()
assert 'Phase 5.5' in content, 'Missing Phase 5.5 section'
assert 'macro_narrative' in content, 'Missing macro_narrative reference'
assert 'holding_commentary' in content, 'Missing holding_commentary reference'
assert 'method_spread_note' in content, 'Missing method_spread_note reference'
assert 'generate_report(results, macro, portfolio, charts' in content, 'Missing updated generate_report call'
# Check section order: 5.5 comes after Phase 5 and before Phase 6
idx_5 = content.index('Phase 5: Visualization')
idx_55 = content.index('Phase 5.5')
idx_6 = content.index('Phase 6: Report')
assert idx_5 < idx_55 < idx_6, 'Section order wrong: must be 5 -> 5.5 -> 6'
print('[OK] CLAUDE.md structure verified')
"</automated>
  </verify>
  <done>
    - CLAUDE.md contains Phase 5.5 section with macro_narrative, holding_commentary, method_spread_note instructions
    - Phase 5.5 instructs Claude to cite actual ERP and P/E values
    - Phase 5.5 specifies 5% threshold for holding commentary
    - Phase 5.5 specifies 5pp threshold for method spread disclosure
    - Phase 6 generate_report() call includes all three narrative params
    - Section ordering is correct: 5 -> 5.5 -> 6
    - No external API calls added
  </done>
</task>

</tasks>

<verification>
python -m pytest tests/test_qual.py::test_readme_cowork_section -x -q && python -c "
content = open('CLAUDE.md', encoding='utf-8').read()
assert 'Phase 5.5' in content
assert 'macro_narrative' in content
print('[OK] All Phase 4 Plan 02 verifications pass')
"
</verification>

<success_criteria>
- CLAUDE.md has Phase 5.5 qualitative narrative generation step
- CLAUDE.md Phase 6 call passes narrative params to generate_report()
- README.md has Cowork setup section with clone, link, upload steps
- test_readme_cowork_section passes
- No external API calls or new library imports added
</success_criteria>

<output>
After completion, create `.planning/phases/04-qualitative-narrative-layer/04-02-SUMMARY.md`
</output>
