# AGENTS.md

Codex bridge instructions for this repository.

- Read `CLAUDE.md` in this same directory before making substantive changes.
- Treat `CLAUDE.md` as the primary repo-specific guidance document for architecture, workflows, commands, constraints, and safety notes.
- Recognize that `CLAUDE.md` was written for Claude Code, so some Claude-specific features or commands may not exist in Codex.
- When a `CLAUDE.md` instruction maps cleanly to Codex, follow it.
- When a `CLAUDE.md` instruction depends on Claude-only features, follow the intent but use the closest clean Codex-native approach instead of a hack.

